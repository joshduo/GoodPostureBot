var twitch = {
    // Twitch username to login with
    username: "USERNAME_HERE",
    // Twitch OAuth token (generated from https://twitchapps.com/tmi/)
    oauthToken: "TOKEN_HERE",
    // Channel(s) to connect to
    channels: ["CHANNEL_HERE", "CHANNEL2_HERE"]
};

var options = {
    // Log executed commands
    commandLogging: true
};

exports.twitch = twitch;
exports.options = options;